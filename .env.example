# Database Configuration
DB_PASSWORD=secure_password_change_me
DATABASE_URL=postgresql://erp_user:secure_password_change_me@localhost:5432/erp_db

# Redis Configuration
REDIS_PASSWORD=redis_password_change_me
REDIS_URL=redis://:redis_password_change_me@localhost:6379

# JWT Configuration
JWT_SECRET=super_secret_jwt_key_change_me_at_least_32_characters_long
JWT_REFRESH_SECRET=super_secret_refresh_key_change_me_at_least_32_characters_long
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Application Configuration
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# Security Configuration
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Email Configuration (for future use)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@erp.local

# PgAdmin Configuration (development only)
PGADMIN_EMAIL=admin@erp.local
PGADMIN_PASSWORD=admin_password_change_me

# Monitoring Configuration (for future use)
SENTRY_DSN=
PROMETHEUS_PORT=9090